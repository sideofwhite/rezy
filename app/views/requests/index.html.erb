<p id="notice"><%= notice %></p>

<h1>Requests</h1>

<table>
  <thead>
    <tr>
      <th>Salary</th>
      <th>Occupation</th>
      <th>Smoker</th>
      <th>Petowner</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @requests.each do |request| %>
      <tr>
        <td><%= request.salary %></td>
        <td><%= request.occupation %></td>
        <td><%= request.smoker %></td>
        <td><%= request.petowner %></td>
      
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= @rental.requests.count %>
<% @rental.requests.each do |request| %>

<% end %>